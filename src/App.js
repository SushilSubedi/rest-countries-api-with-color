import React from 'react';
import { Box } from '@material-ui/core';
import Routers from './Router/Router';

const App = (props) => {
  return(
    <Box>
        <Routers/>
    </Box>
  )
}
export default App;
